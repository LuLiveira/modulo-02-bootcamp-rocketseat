import mongooose from 'mongoose';

const NotificationSchema = new mongooose.Schema(
  {
    content: {
      type: String,
      required: true,
    },
    user: {
      type: Number,
      required: true,
    },
    read: {
      type: Boolean,
      required: true,
      default: false,
    },
  },
  {
    timestamps: true,
  }
);

export default mongooose.model('Notification', NotificationSchema);
